{
  "name": "stock-ledger",
  "version": "1.0.0",
  "description": "Offline shoe / sandal / slipper stock ledger — Excel-style grid with photo cells",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "add:android": "npx cap add android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/camera": "^6.0.2",
    "@capacitor-community/sqlite": "^6.0.2",
    "jeep-sqlite": "^2.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
